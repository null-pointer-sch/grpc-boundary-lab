FROM golang:1.26-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -buildvcs=false -o /gateway ./cmd/gateway

FROM alpine:3.19
COPY --from=builder /gateway /gateway
EXPOSE 50052
ENTRYPOINT ["/gateway"]
